# Profiles

This directory is for optional local project profiles.

Publishing rule:

- keep `template.json` committed as the example
- keep real local profiles untracked

That is why `.gitignore` ignores `bridge/profiles/*.json` except `bridge/profiles/template.json`.
